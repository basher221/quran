{
  "name": "quran-mobile-app",
  "version": "1.0.0",
  "private": true,
  "description": "Quran web app packaged as Android app using Capacitor.",
  "scripts": {
    "build:web": "powershell -ExecutionPolicy Bypass -File ./prepare-mobile-web.ps1",
    "cap:sync": "npm run build:web && npx cap sync",
    "cap:copy": "npm run build:web && npx cap copy",
    "cap:open:android": "npm run build:web && npx cap open android",
    "cap:add:android": "npx cap add android"
  },
  "dependencies": {
    "@capacitor/android": "8.1.0",
    "@capacitor/core": "8.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.1.0"
  }
}
